import { PluginOption } from 'vite';

declare function vitePluginHttpBasicAuth(users: {
    username?: string;
    password: string;
}[], config?: {
    realm?: string;
    useInServer?: boolean;
    useInPreview?: boolean;
}): PluginOption;

export { vitePluginHttpBasicAuth as default };
